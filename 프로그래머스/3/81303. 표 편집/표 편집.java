class Solution {
    public String solution(int n, int k, String[] cmd) {
        int[] map = new int[n]; // 0으로 초기화
        StringTokenizer st;
        
        for (int i = 0; i < cmd.length(); i++) {
            st = new StringTokenizer(cmd[i]);
            String temp = st.nextToken();
            switch (temp) {
                case : "U" {
                    Integer.parseInt(st.nextToken());
                }
            }
        }
        String answer = "";
        return answer;
    }
}